export const hosts = {
    mongoDb_main: {
        host: 'mongodb://localhost/test1',
        options: {
            reconnectTries: Number.MAX_VALUE
        }

    }
}

export const key = {
    secret: 'Ifes-secret-key'
}